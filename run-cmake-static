#!/bin/bash
#
# Configure a statically linked release build (CLP backend) in static/.
# Requires static versions of the Coin-OR libraries.

source "$(cd "$(dirname "$0")" && pwd)/common.sh"

configure static -DSTATIC_COLIN=TRUE -DCMAKE_BUILD_TYPE=Release
